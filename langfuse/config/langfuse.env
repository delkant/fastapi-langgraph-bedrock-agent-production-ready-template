# Langfuse Configuration
# This file contains environment-specific configuration for Langfuse

# Database Configuration
LANGFUSE_DATABASE_URL=postgresql://langfuse:langfuse_password@langfuse-db:5432/langfuse

# Authentication Configuration
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=langfuse_secret_key_change_in_production

# Salt for hashing (change in production)
SALT=langfuse_salt_change_in_production

# Server Configuration
NODE_ENV=production
PORT=3000

# Features
LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES=true

# Optional: Email Configuration (uncomment and configure if needed)
# SMTP_CONNECTION_URL=smtps://username:password@smtp.example.com:465

# Optional: S3 Configuration for file uploads (uncomment and configure if needed)
# S3_ENDPOINT=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_BUCKET_NAME=
# S3_REGION=

# Optional: Disable telemetry
TELEMETRY_ENABLED=false

# CORS Configuration (for development)
CORS_ALLOWED_ORIGINS=http://localhost:3001,http://localhost:8000